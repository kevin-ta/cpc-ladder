{% extends "::base.html.twig" %}

{% block title %}- Nouveau profil{% endblock %}

{% block body %}
    <div class="col-md-12">
        <h4>Il semble que vous n'ayez pas de profil sur {{ videogame.name }} ...</h4>
        <h4>Nous vous proposons d'en créer un :</h4><br><br>
        <form method="post" class="form-horizontal">
            <div class="col-md-12">
                <div class="form-group">
                    {{ form_label(form.nickname, label|default(null), {'label_attr': {'class':'col-md-2 col-md-offset-2 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(form.nickname, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
            <div class="col-md-12">
                <div class="form-group">
                    {{ form_label(form.team, label|default(null), {'label_attr': {'class':'col-md-2 col-md-offset-2 control-label'}}) }}
                    <div class="col-md-6">
                        {{ form_widget(form.team, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-md-offset-3">
                <br><p>Si tu souhaites créer ta propre équipe, clique sur <b>Créer mon équipe</b>.<br>Si tu n'as pas d'amis, tu peux juste envoyer le form en ne sélectionnant pas d'équipe.</p><br>
                <button type="submit" class="btn btn-success" value="no_team" id="send" name="submit">Envoyer</button>
                <button type="submit" class="btn btn-primary" value="team" id="send" name="submit">Créer mon équipe</button>
            </div>
        </form>
    </div>
{% endblock body %}