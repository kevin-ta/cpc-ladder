{% extends "::base.html.twig" %}

{% block body %}
    <div class="col-md-12">
        <h4>Il semble que vous n'ayez pas d'équipe sur {{ videogame.name }} ...</h4>
        <h4>Nous vous proposons d'en créer une:</h4><br>
        <form method="post" class="form-horizontal">
            <div class="col-md-12">
                <div class="form-group">
                    <label for="new" class="col-md-2 col-md-offset-2 control-label">Nom de l'équipe</label>
                    <div class="col-md-6">
                        <input type="text" class="form-control" id="new" name="new">
                    </div>
                </div><br>
            </div>
            <div class="col-md-9 col-md-offset-3">
                <button type="submit" class="btn btn-primary" value="new" id="send" name="submit">Créer mon équipe</button><br><br><br>
            </div>
            <h4>Sinon tu peux toujours en rejoindre une :</h4><br>
            <div class="col-md-12">
                <div class="form-group">
                    <label for="new" class="col-md-2 col-md-offset-2 control-label">Equipes existantes</label>
                    <div class="col-md-6">
                        <select class="form-control" name="team">
                            {% for team in teams %}
                                <option value="{{ team.id }}">{{ team }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
            </div>
            <div class="col-md-6 col-md-offset-3">
                <br><button type="submit" class="btn btn-success" value="exist" id="send" name="submit">Envoyer</button>
            </div>
        </form>
    </div>
{% endblock %}